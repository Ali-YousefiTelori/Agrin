<UserControl x:Class="Agrin.Windows.UI.Views.Controls.SmallIconControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="33" d:DesignWidth="33" x:Name="mainControl" FlowDirection="LeftToRight">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Style="{DynamicResource toolbarButtonStyleNoneBorder}" Content="{Binding ContentStyle,ElementName=mainControl}" Grid.ColumnSpan="2" Grid.RowSpan="2"/>
        <Ellipse Grid.Column="1" Fill="White" Stroke="Red" StrokeThickness="0.5">
            
        </Ellipse>
        <Button Style="{DynamicResource toolbarButtonStyleNoneBorder}" Content="{DynamicResource Close_TemplateStyle}" Foreground="#FFB40000" Grid.Column="1"/>

    </Grid>
</UserControl>

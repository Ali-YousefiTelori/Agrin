using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;

namespace Agrin.Helpers
{
    //public class CustomAlertDialog : AlertDialog
    //{
    //    public Activity CurrentActivity { get; set; }
    //    public CustomAlertDialog(Context context, bool cancelable, EventHandler cancelHandler) : base(context, cancelable, cancelHandler)
    //    {
    //        CurrentActivity = (Activity)context;
    //    }

    //    public override void OnContextMenuClosed(IMenu menu)
    //    {
    //        CurrentActivity.OnContextMenuClosed(menu);
    //    }

    //    public override bool OnContextItemSelected(IMenuItem item)
    //    {
    //        return CurrentActivity.OnContextItemSelected(item);
    //    }

    //    public override void OnCreateContextMenu(IContextMenu menu, View v, IContextMenuContextMenuInfo menuInfo)
    //    {
    //        CurrentActivity.OnCreateContextMenu(menu, v, menuInfo);
    //    }
    //}
}
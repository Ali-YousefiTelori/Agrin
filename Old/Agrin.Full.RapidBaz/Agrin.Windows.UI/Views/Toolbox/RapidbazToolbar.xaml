<UserControl x:Class="Agrin.Windows.UI.Views.Toolbox.RapidbazToolbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="27" d:DesignWidth="300" FlowDirection="LeftToRight" x:Name="mainControl">
 
    <StackPanel Orientation="Horizontal">
        <Button Style="{DynamicResource toolbarButtonStyle}" Command="{Binding RefreshCommand}" Content="{DynamicResource Refresh_TemplateStyle}" Padding="6"/>
        <Button Style="{DynamicResource toolbarButtonStyle}" Command="{Binding AddLinksCommand}" Content="{DynamicResource Add_TemplateStyle}"/>
        <Button Style="{DynamicResource toolbarButtonStyle}" Command="{Binding DeleteLinksCommand}" Content="{DynamicResource Trash_TemplateStyle}" Padding="6" Margin="5,0,0,0"/>
    </StackPanel>
</UserControl>
